#include <autoconf.h>

#ifdef CONFIG_FILE_SYSTEM_LITTLEFS
littlefs_storage:
  placement: {before: [end]}
#ifdef CONFIG_PM_EXTERNAL_FLASH_LITTLEFS
  region: external_flash
#endif
  size: CONFIG_PM_PARTITION_SIZE_LITTLEFS
#endif
